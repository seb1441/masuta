<!DOCTYPE html>
<html>
  <head>
    <title>Masuta</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", media: "all", "data-turbo-track": "reload" %>
    <%= javascript_packs_with_chunks_tag "application" %>
    <%= stylesheet_packs_with_chunks_tag "application", media: "all", "data-turbo-track": "reload" %>

    <meta name="viewport" content="width=device-width, initial-scale=1">
  </head>

  <body>
    <%= render partial: "layouts/navbar" %>
    <%= render partial: "layouts/notif", locals: { bg_color: "bg-blue-400", border_color: "border-blue-700", message: notice } if notice %>
    <%= render partial: "layouts/notif", locals: { bg_color: "bg-red-400", border_color: "border-red-700", message: alert } if alert %>

    <%= yield %>

    <footer class="text-gray-700 body-font">
      <div class="bg-gray-200">
        <div class="container mx-auto py-4 px-5 flex flex-wrap flex-col sm:flex-row">
          <p class="text-gray-500 text-sm text-center sm:text-left">© 2020 Masuta
          </p>
          <span class="sm:ml-auto sm:mt-0 mt-2 sm:w-auto w-full sm:text-left text-center text-gray-500 text-sm">Yukiko</span>
        </div>
      </div>
    </footer>
  </body>
</html>
